project:
  name: jabtop
  description: AI agent monitor for your terminal — like btop, but for AI coding agents
  longDescription: |
    Monitor Claude Code, Codex CLI, pi.dev, and opencode sessions in a single TUI dashboard.
    Shows token usage, context window, rate limits, child processes, and more.
    Java port of the Rust abtop project.
  authors:
    - Max Rydahl Andersen
  license: MIT
  links:
    homepage: https://github.com/maxandersen/jabtop
    documentation: https://github.com/maxandersen/jabtop#readme
  languages:
    java:
      groupId: dev.abtop
      artifactId: jabtop
      mainClass: dev.abtop.AbtopCommand
      version: 21
  inceptionYear: "2026"
  snapshot:
    label: early-access
    fullChangelog: true

release:
  github:
    owner: maxandersen
    name: jabtop
    overwrite: true
    update:
      enabled: true
      sections:
        - ASSETS
    tagName: "v{{projectVersion}}"
    releaseName: "v{{projectVersion}}"
    changelog:
      formatted: ALWAYS
      preset: conventional-commits
      contributors:
        enabled: true
      hide:
        categories:
          - merge

distributions:
  jabtop:
    type: SINGLE_JAR
    artifacts:
      - path: release/jabtop-{{projectVersion}}.jar
